# Browser key from the Yandex Developer Dashboard → JavaScript API.
# Restrict it to localhost and your eventual production domain.
VITE_YANDEX_MAPS_API_KEY=

# Optional: a clearly labelled local preview that never calls Yandex routing.
VITE_DEMO_MODE=false
